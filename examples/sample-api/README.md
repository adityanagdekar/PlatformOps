# sample-api

This directory is reserved for the Phase 2 deployable backend service.

Planned endpoints:

- `GET /health`
- `GET /ready`
- `GET /metrics`

Planned runtime assets:

- `Dockerfile`
- Kubernetes `Deployment`
- Kubernetes `Service`
- Kubernetes `ConfigMap`
- readiness and liveness probes
